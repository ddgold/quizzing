{
	"name": "quizzing",
	"version": "0.1.0",
	"description": "easy to build and share trivia boards",
	"private": true,
	"repository": "git@github.com:ddgold/quizzing.git",
	"author": "Doug Goldstein <ddgold@me.com>",
	"license": "GPL-3.0-only",
	"workspaces": [
		"server",
		"web"
	],
	"devDependencies": {
		"concurrently": "^6.0.0"
	},
	"scripts": {
		"dev": "concurrently -n \"server, web  ,\" -c \"bgGreen,bgBlue\"  \"yarn workspace @quizzing/server dev\" \"yarn workspace @quizzing/web dev\"",
		"remove-build": "yarn workspaces foreach run remove-build",
		"compose": "docker-compose up -d --build",
		"create-mongo": "docker run -it -v /Volumes/mongodata:/data/db -p 27017:27017 --name mongo -d mongo:4.4.1"
	}
}
