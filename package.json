{
	"name": "quizzing",
	"version": "0.1.0",
	"description": "easy to build and share trivia boards",
	"private": true,
	"repository": "git@github.com:ddgold/quizzing.git",
	"author": "Doug Goldstein <ddgold@me.com>",
	"license": "GPL-3.0-only",
	"workspaces": [
		"server",
		"web"
	],
	"devDependencies": {
		"concurrently": "^5.3.0"
	},
	"scripts": {
		"dev": "concurrently -n \"server, web  ,\" -c \"bgGreen,bgBlue\"  \"yarn --cwd server dev\" \"yarn --cwd web dev\"",
		"compose": "docker-compose up -d --build",
		"create-mongo": "docker run -it -v /Volumes/mongodata:/data/db -p 27017:27017 --name mongo -d mongo:4.4.1",
		"remove-modules": "rm -rf ./node_modules && yarn --cwd server remove-modules && yarn --cwd web remove-modules"
	}
}
